{
  "name": "room2store",
  "private": true,
  "type": "module",
  "scripts": {
    "dev:perception": "node --env-file-if-exists=.env services/perception/src/server.mjs",
    "linq:subscribe": "node --env-file-if-exists=.env scripts/subscribe-webhook.mjs",
    "test": "node --test \"services/*/test/**/*.test.mjs\""
  }
}
